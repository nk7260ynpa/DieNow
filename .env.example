# Claude Code CLI 相關設定 (change `migrate-to-claude-cli-subprocess`).
#
# 認證 (必填其一):
# - CLAUDE_CODE_OAUTH_TOKEN: 推薦. 於主機執行 `claude setup-token` 產生
#   long-lived token 後填入; 走 Max 訂閱計費, 可跨主機/容器使用.
# - ANTHROPIC_API_KEY: 走 API key 計費.
#
# 2026-04-18 後不再依賴 `claude login` 建立的 ~/.claude/ 目錄:
# macOS 上該目錄僅含 metadata, 實際 OAuth token 存於 Keychain, 無法跨
# 到 Linux 容器. `claude setup-token` 產生的 token 透過 env 注入才能
# 真正讓容器內的 `claude` 承繼 Max 訂閱身份.
CLAUDE_CODE_OAUTH_TOKEN=

# 可選: API key 後備認證 (若未啟用 Max 訂閱或另有計費需求).
# ANTHROPIC_API_KEY=

# `claude` 可執行檔路徑 (容器內已由 Dockerfile 安裝於 PATH).
CLAUDE_CLI_PATH=claude
CLAUDE_CLI_TIMEOUT_SECONDS=30
# 向後相容保留欄位; 2026-04-18 後不再用於預啟動檢查, 僅 scenario config
# 仍解析此值. 留空即可.
CLAUDE_HOME=

# Project Agent 預設模型; 可填 claude-sonnet-4-*, claude-opus-4-*,
# claude-haiku-4-*; 留空代表走 CLI 預設.
PROJECT_AGENT_MODEL=claude-sonnet-4-7

# structlog 等級.
LOG_LEVEL=INFO
